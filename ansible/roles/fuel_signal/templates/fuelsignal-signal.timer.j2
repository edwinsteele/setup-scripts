# Managed by ansible - roles/fuel_signal
[Unit]
Description=Periodic fuel-price-signal fetch + signal check

[Timer]
OnCalendar={{ fuel_signal_timer_oncalendar }}
Persistent=true

[Install]
WantedBy=timers.target
