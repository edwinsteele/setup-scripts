# Managed by ansible - roles/fuel_signal
[Unit]
Description=Daily fuel-price-signal git pull, DB update, and signal check

[Timer]
OnCalendar={{ fuel_signal_daily_update_oncalendar }}
Persistent=true

[Install]
WantedBy=timers.target
