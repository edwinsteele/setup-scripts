# Managed by ansible - roles/home_assistant, but only seeded once: this
# file is only written if it doesn't already exist, so re-running the
# playbook never clobbers hand edits or what the UI has written into it.
default_config:

homeassistant:
  time_zone: {{ home_assistant_timezone }}
