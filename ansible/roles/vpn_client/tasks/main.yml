---
- name: Copy iked.conf
  ansible.builtin.copy:
    src: iked.conf
    dest: /etc/iked.conf
    mode: 0600

- name: Enable and start iked
  ansible.builtin.service:
    name: iked
    state: started
    enabled: true

